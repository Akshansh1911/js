const accountId = 12345;
var accountName = "new";
let accountdetails = "hello";
let hellouser;

//always use let for variable because var has some block and functional scope issue 


/*
and if u dont give any value in the var or const it automatically considers undefined

nice op
*/






console.log(accountId);
console.table([accountId, accountName, accountdetails,hellouser]);